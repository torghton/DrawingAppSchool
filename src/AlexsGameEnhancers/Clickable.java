package AlexsGameEnhancers;

public interface Clickable {
    void mouseClicked(Vector Position, int mouseButton);

    void mousePressed(Vector Position, int mouseButton);

    void mouseReleased(Vector Position, int mouseButton);
}
